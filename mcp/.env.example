# Base URL for the Auth/Validation/Stock backend this server proxies to —
# the nginx gateway from ../services/docker-compose.yml by default.
# Required; the server throws on the first tool call if this is unset.
API_BASE_URL=http://localhost:8080
